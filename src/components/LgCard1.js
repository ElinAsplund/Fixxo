import React from 'react'

const LgCard = ({heading}) => {
  return (
    <div className="two-for-holder" id="lg-card-1">
        <h2>{heading}</h2>
        <button className="btn-bg-white">FLASH SALE</button>
    </div>  
  )
}

export default LgCard